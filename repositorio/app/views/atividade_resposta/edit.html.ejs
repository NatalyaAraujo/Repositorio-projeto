<div class="hero-unit">
  <form id="atividadeRespostum-form" class="form-horizontal" action="/atividade_resposta/<%= params.id %>?_method=PUT" method="POST">
    <fieldset>
      <legend>Update this AtividadeRespostum</legend>
      <% if(params.errors) { %>
      <div class="control-group">
        <ul>
        <% for(var err in params.errors) { %>
          <li><%= params.errors[err]; %></li>
        <% } %>
        </ul>
      </div>
      <% } %>

      <%- partial('form', {atividadeRespostum: atividadeRespostum}) %>

      <div class="form-actions">
        <%- contentTag('input', 'Save', {type: 'submit', class: 'btn btn-primary'}) %>
        <%- contentTag('button', 'Remove', {type: 'submit', formaction: '/atividade_resposta/' + params.id + '?_method=DELETE', formmethod: 'POST', class: 'btn btn-danger'}) %>
      </div>
    </fieldset>
  </form>
</div>

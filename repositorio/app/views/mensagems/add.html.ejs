<div class="hero-unit">
  <form id="mensagem-form" class="form-horizontal" action="/mensagems" method="POST">
    <fieldset>
      <legend>Create a new Mensagem</legend>
      <% if(params.errors) { %>
      <div class="control-group">
        <ul>
        <% for(var err in params.errors) { %>
          <li><%= params.errors[err]; %></li>
        <% } %>
        </ul>
      </div>
      <% } %>

      <%- partial('form', {mensagem: params, destinatarios: destinatarios}) %>

      <div class="form-actions">
        <%- contentTag('input', 'Add', {type: 'submit', class: 'btn btn-primary'}) %>
      </div>
    </fieldset>
  </form>
</div>

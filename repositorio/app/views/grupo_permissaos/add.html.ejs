<div class="hero-unit">
  <form id="grupoPermissao-form" class="form-horizontal" action="/grupo_permissaos" method="POST">
    <fieldset>
      <legend>Create a new GrupoPermissao</legend>
      <% if(params.errors) { %>
      <div class="control-group">
        <ul>
        <% for(var err in params.errors) { %>
          <li><%= params.errors[err]; %></li>
        <% } %>
        </ul>
      </div>
      <% } %>

      <%- partial('form', {grupoPermissao: params, perfils: perfils, entidades: entidades, permissaos: permissaos}) %>

      <div class="form-actions">
        <%- contentTag('input', 'Add', {type: 'submit', class: 'btn btn-primary'}) %>
      </div>
    </fieldset>
  </form>
</div>

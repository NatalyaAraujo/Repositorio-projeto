<div class="hero-unit">
  <form id="atividade-form" class="form-horizontal" action="/atividades" method="POST">
    <fieldset>
      <legend>Create a new Atividade</legend>
      <% if(params.errors) { %>
      <div class="control-group">
        <ul>
        <% for(var err in params.errors) { %>
          <li><%= params.errors[err]; %></li>
        <% } %>
        </ul>
      </div>
      <% } %>

      <%- partial('form', {atividade: params, turmaId: turmaId}) %>

      <div class="form-actions">
        <%- contentTag('input', 'Add', {type: 'submit', class: 'btn btn-primary'}) %>
      </div>
    </fieldset>
  </form>
</div>

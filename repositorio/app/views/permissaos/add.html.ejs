<div class="hero-unit">
  <form id="permissao-form" class="form-horizontal" action="/permissaos" method="POST">
    <fieldset>
      <legend>Create a new Permissao</legend>
      <% if(params.errors) { %>
      <div class="control-group">
        <ul>
        <% for(var err in params.errors) { %>
          <li><%= params.errors[err]; %></li>
        <% } %>
        </ul>
      </div>
      <% } %>

      <%- partial('form', {permissao: params, entidades: entidades}) %>

      <div class="form-actions">
        <%- contentTag('input', 'Add', {type: 'submit', class: 'btn btn-primary'}) %>
      </div>
    </fieldset>
  </form>
</div>

<div class="hero-unit">
  <form id="usuario-form" class="form-horizontal" action="/usuarios" method="POST">
    <fieldset>
      <legend>Create a new Usuario</legend>
      <% if(params.errors) { %>
      <div class="control-group">
        <ul>
        <% for(var err in params.errors) { %>
          <li><%= params.errors[err]; %></li>
        <% } %>
        </ul>
      </div>
      <% } %>

      <%- partial('form', {usuario: params, perfils: perfils}) %>

      <div class="form-actions">
        <%- contentTag('input', 'Add', {type: 'submit', class: 'btn btn-primary'}) %>
      </div>
    </fieldset>
  </form>
</div>

<div class="hero-unit">
  <form id="perfil-form" class="form-horizontal" action="/perfils" method="POST">
    <fieldset>
      <legend>Create a new Perfil</legend>
      <% if(params.errors) { %>
      <div class="control-group">
        <ul>
        <% for(var err in params.errors) { %>
          <li><%= params.errors[err]; %></li>
        <% } %>
        </ul>
      </div>
      <% } %>

      <%- partial('form', {perfil: params, permissaos: permissaos, entidades: entidades}) %>

      <div class="form-actions">
        <%- contentTag('input', 'Add', {type: 'submit', class: 'btn btn-primary'}) %>
      </div>
    </fieldset>
  </form>
</div>
